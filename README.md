# Lux Knowledge Platform

TypeScript MCP server for CORPUS semantic search and knowledge retrieval.

## Features

- Semantic search over CORPUS markdown files
- MCP-native interface (stdio + HTTP)
- Local vector embeddings
- Git-aware indexing

## Stack

- TypeScript / Node.js
- MCP SDK
- Local embedding model (gte-small or similar)

